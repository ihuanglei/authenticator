package message

import (
	"testing"

	. "github.com/smartystreets/goconvey/convey"
)

func TestEmail(t *testing.T) {
	Convey("test email", t, func() {
		// ev := EmailValues{}
		// ev.Host = "smtp.ym.163.com"
		// ev.Port = 465
		// ev.From = ""
		// ev.Username = ""
		// ev.Password = ""
		// ev.Subject = "test"
		// ev.Body = "test"
		// ev.To = []string{""}
		// err := NewEmailMessage().Send(ev)
		// So(err, ShouldBeNil)
	})
}
